# Consolidated local-dev dependencies (model + web + notebooks).
# The model and web runtime deps live in their own files so the Docker
# image can install only what it needs:
#   - src/requirements-model.txt  (2D shallow-water screening model)
#   - src/web/requirements.txt    (Flask + MapLibre web service)
#   - this file                   (everything, for local development)

-r requirements-model.txt
-r web/requirements.txt

# --- Notebooks ---
jupyter>=1.0
matplotlib>=3.7
